/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    compiler: {
        styledComponents: true, // Habilita soporte nativo para styled-components
    },
};

export default nextConfig;
